import React, { FC } from 'react'

import { DebugSelectedDataInfo } from '../../Debug/DebugSelectedDataInfo'

export const VisualizationReference: FC = () => {
  return (
    <section>
      <DebugSelectedDataInfo />
    </section>
  )
}
